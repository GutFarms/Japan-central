# Copied to config/appliance.env by setup.sh (gitignored).
# Prefer editing .env in the appliance root; this file is an alternate
# location some hosts mount as a secrets volume.

CURSOR_API_KEY=
CURSOR_APPLIANCE_NAME=cursor-appliance
CURSOR_APPLIANCE_MANAGEMENT_ADDR=127.0.0.1:8733
