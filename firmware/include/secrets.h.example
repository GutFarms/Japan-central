#pragma once

// Copy to secrets.h (or edit the tracked secrets.h) and set credentials.
#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

#define METRICS_UDP_PORT 4210

// Optional: milliseconds without a packet before the UI shows WAIT.
// #define METRICS_STALE_MS 3000
