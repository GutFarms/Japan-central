#pragma once

// Copy this file to secrets.h and fill in your network credentials.
// secrets.h is gitignored and pulled in by wifi_config.h when present.

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// UDP port the firmware listens on for metric packets from the host agent.
#define METRICS_UDP_PORT 4210

// Optional: milliseconds without a packet before the UI shows WAIT.
// #define METRICS_STALE_MS 3000
