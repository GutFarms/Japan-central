[package]
name = "jchc-hup"
version = "0.1.0"
edition = "2021"
description = "JCHC-1 Hasher UART Protocol reference codec (hardware companion, not the miner)"
license = "MIT OR Apache-2.0"
publish = false

[lib]
path = "src/lib.rs"

[dev-dependencies]
